version: "3.8"
networks:
  ocrcccd:
    driver: bridge
services:
  ocrcccd:
    build: .
    volumes:
       - ./ocrcccd:/usr/src/app/
    ports:
       - 80:5555
    restart: always
    networks:
      - ocrcccd
